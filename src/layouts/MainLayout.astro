---
import "@/styles/global.css";
import { Button } from "@/components/ui/button"
---

<script>
  const button = document.querySelector('[sidebar-trigger]');
  button?.addEventListener('click', (e) => {
    console.log('hellou!!', {e});
  })
</script>

<html lang="en" style="overscroll-behavior: none">
<head>
  <meta charset="utf-8" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<meta name="viewport" content="width=device-width" />
<meta name="generator" content={Astro.generator} />
<title>Astro</title>
</head>
<body class="min-h-screen md:flex">
  <aside class="fixed flex flex-col mt-0 top-0 px-5 left-0 bg-zinc-100 text-zinc-900 h-screen transition-all duration-300 ease-in-out z-99999 border-r border-gray-200 md:w-[200px] -translate-x-full md:translate-x-0" >
    here goes sidebar
  </aside>
  <div class="flex-1 transition-all duration-300 ease-in-out md:ml-[200px]">
    <header class="sticky top-0 flex w-full h-16 bg-white border-b border-zinc-300 z-99999 items-center px-2 gap-x-4">
        <Button>Trigger</Button>
        <button
          sidebar-trigger
          class="cursor-pointer hover:bg-zinc-100">
          []
        </button>
        Here goes header
    </header>
    <main class="p-4 mx-auto max-w-(--breakpoint-2xl) md:p-6">
      <slot />
    </main>
  </div>
</body>
</html>
